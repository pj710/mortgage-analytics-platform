environment: development

aws:
  region: us-east-1
  s3_bucket: jo-map
  s3_prefix: jo-map

databricks:
  catalog: map
  bronze_schema: bronze
  core_schema: core

paths:
  data_root: data
  freddie_data: data/freddie_raw
  originations_data: data/freddie_raw/originations
  performance_data: data/freddie_raw/performance
  headers: data/headers
